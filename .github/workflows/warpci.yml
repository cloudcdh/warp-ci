name: Sending Data

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - main
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
       - name: Checkout
         uses: actions/checkout@main

       - name: Download Script
         run: |
           wget https://raw.githubusercontent.com/Area69Lab/warp-script/main/warp.py
           sudo apt update && sudo apt install -y python3 python-pip
           pip3 install wheel setuptools requests
       - name: Running Script
         run: python3 warp.py
